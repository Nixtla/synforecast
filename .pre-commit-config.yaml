repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.0
    hooks:
      - id: ruff
        files: ^(synforecast|tests|examples|docs|benchmarks)/
        args: [--fix]
      - id: ruff-format
        files: ^(synforecast|tests|examples|docs|benchmarks)/
