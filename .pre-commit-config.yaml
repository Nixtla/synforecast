repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.22
    hooks:
      - id: ruff
        files: ^(synforecast|tests|examples|docs)/
        args: [--fix]
      - id: ruff-format
        files: ^(synforecast|tests|examples|docs)/
